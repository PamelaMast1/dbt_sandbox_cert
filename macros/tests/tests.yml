macros:
  - name: not_null
    description: Custom override to test `not_null` test as it checks that there a is deleted flag
    # arguments:
    #   - name: model 
    #     type: string
    #     description: Model Name
    #   - name: column_name
    #     type: string
    #     description: Column name that should not be an empty string
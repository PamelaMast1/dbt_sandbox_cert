packages:
  - package: dbt-labs/dbt_utils
    version: 1.3.2

  # Example of how a private/internal Git package would be declared.
  # This is commented out so dbt doesn't try to install it in dbt Cloud.
  # - git: "https://github.com/blah blah blah/lifestyle_ingestion.git"
  #   revision: main
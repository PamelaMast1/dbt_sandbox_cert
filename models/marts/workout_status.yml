version: 2

models:
  - name: workout_status
    description: Test model for workout statuses
    columns:
      - name: status
        description: Workout lifecycle status
        tests:
          - accepted_values:
              config:
                severity: warn
              arguments:
                values: ['pending', 'completed', 'cancelled']
                quote: true